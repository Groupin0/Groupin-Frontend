import React from "react";

const style = {textAlign: 'center', fontSize: '3.5rem', width: '100%', transform: 'translateY(50px)'};

const NoSessions = () => <div style={style}>לא נמצאו פגישות</div>;

export default NoSessions;